[package]
name = "grpc-service"
version = "0.1.0"
edition = "2021"

[dependencies]
common = { path = "../common" }
tonic = "0.12"
prost = "0.13"

[build-dependencies]
tonic-build = "0.12"
prost-build = "0.13"
